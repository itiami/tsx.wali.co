1. NPM to run the Typescript....
   "test": "echo \"Typescript in Node Express Framework\"",
   "build": "npx tsc",
   "start": "nodemon --watch 'src/\*_/_.ts' --exec 'ts-node' src/server.ts"

2. Controller - A reusable Controller for all Entity
